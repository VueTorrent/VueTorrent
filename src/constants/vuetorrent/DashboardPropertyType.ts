export enum DashboardPropertyType {
  AMOUNT = 'amount',
  BOOLEAN = 'boolean',
  CHIP = 'chip',
  DATA = 'data',
  DATETIME = 'datetime',
  DURATION = 'duration',
  PERCENT = 'percent',
  RELATIVE = 'relative',
  SPEED = 'speed',
  TEXT = 'text'
}
