export enum DashboardPropertyType {
  AMOUNT = 'amount',
  CHIP = 'chip',
  DATA = 'data',
  DATETIME = 'datetime',
  DURATION = 'duration',
  PERCENT = 'percent',
  RELATIVE = 'relative',
  SPEED = 'speed',
  TEXT = 'text'
}
